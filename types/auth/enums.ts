export enum AccountTypes {
  consumer = 'consumer',
  shop = 'shop',
  supplier = 'supplier',
}

export enum MenuItemTypes {
  menu = 'menu',
  divider = 'divider',
}

export enum MenuTypes {
  account = 'account',
  profile = 'profile',
}
